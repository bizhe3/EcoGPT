# EcoGPT - A100 80G specific dependencies
# Hardware: 2×A100 80G (Ampere, compute capability 8.0)
# CUDA: 12.4
#
# Install:
#   conda create -n ecogpt python=3.10 -y && conda activate ecogpt
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install flash-attn==2.7.2 --no-build-isolation
#   pip install -r requirements-a100.txt
#   pip install -r requirements.txt

torch==2.5.1
vllm==0.6.6.post1
# flash-attn==2.7.2  # 单独安装: pip install flash-attn==2.7.2 --no-build-isolation
